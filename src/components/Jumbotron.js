import React from "react";
import "../styles/Jumbotron.css";


const Jumbotron = () => (
  <div className="jumbotron">
    <h1>Clicky Game!</h1>
    <p>
      Click on an image to score points, but don't click on more than once!
    </p>
  </div>
);

export default Jumbotron;
